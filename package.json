{
  "name": "kanbaam",
  "version": "1.0.0",
  "private": true,
  "description": "A local-first, no-build kanban board. Open index.html to start.",
  "scripts": {
    "build:icons": "node scripts/build-icons.cjs",
    "test": "node --test tests/*.test.cjs",
    "test:browser": "playwright test",
    "verify": "npm run check && npm test && npm run test:browser",
    "check": "node --check model.js && node --check storage.js && node --check background.js && node --check app.js"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "lucide": "^0.468.0"
  }
}
